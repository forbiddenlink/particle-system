{
  "name": "@nova-particles/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "pnpm --filter @nova-particles/core build && tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@nova-particles/core": "workspace:*",
    "posthog-js": "^1.399.4",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/three": "^0.185.1",
    "typescript": "^6.0.3",
    "vite": "^8.1.5"
  }
}
